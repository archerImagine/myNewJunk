
# decimal precision of 3 for float 0.333
print '{0:.3f}'.format(1.0/3)

# fill with underscore with the text centered (^) to 11 width
print '{0:_^11}'.format("hello")

# keyword based

print "{name} wrote {book}".format(name = "archer",book="Nothing")
